# eslint-config-chrono

OneThousand Eslint config with:

- Airbnb config
- Typescript
- Prettier

## Usage

```
npx install-peerdeps --dev @chronoswap/eslint-config-chrono
```

Add `"extends": "@chronoswap/eslint-config-chrono"` to your eslint config file.
